#include<iostream>
#include<string>

using namespace std;

int main ()
{
    string::iterator it;
    string s="hasan";
    //s.insert(s.begin(),'h');
    //s.insert(s.begin()+2,'e');
    //s.insert(s.begin(),3,'h');
    //s.insert(s.begin()+3,7,'h');
    //s.insert(s.end(),'h');

    //s.erase(s.begin());
    //s.erase(s.begin()+1);
    //s.erase(s.begin(),s.end());
    //s.erase(s.begin()+1,s.end());
    //s.erase(s.begin(),s.end()-1);
    //s.erase(s.begin()+1,s.end()-2);
    //s.erase(s.end()-1);

    //s.clear();



    cout << s << endl;
    cout << "string size is " << s.size() << endl;

    return 0;
}
